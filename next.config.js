/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "v5.airtableusercontent.com",
      "dl.airtable.com",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
