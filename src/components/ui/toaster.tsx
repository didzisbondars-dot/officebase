"use client";

export function Toaster() {
  // Minimal toaster - can be replaced with a full solution like sonner
  return <div id="toaster" className="fixed top-4 right-4 z-[100] flex flex-col gap-2" />;
}
